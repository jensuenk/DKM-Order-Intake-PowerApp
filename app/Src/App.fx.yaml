App As appinfo:
    BackEnabled: =false
    OnStart: |-
        =
        Set(varToursFolder, "Gedeelde documenten/WebA/PowerPlatform/DKM Order Intake/Tours/");
        Start;
    StartScreen: =Start
    Theme: =PowerAppsTheme

    Host As hostControl.DefaultHostControlVariant:
        OnCancel: =false
        OnEdit: =false
        OnNew: =false
        OnSave: =false
        OnView: =false

